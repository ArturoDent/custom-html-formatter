src/test/fixtures/
  normalize/
    normalize.config.json        ← folder default

    basic.input.html
    basic.expected.html
    basic.config.json            ← per‑fixture override

    nested.input.html
    nested.expected.html         ← uses folder default

  tabs/
    tabs.config.json

    simple.input.html
    simple.expected.html
    
 
 // dryRun into Diagnostics
if (state === "active" && dryRunEnabled) {
  const preview = getDocumentChanges(original, rules);

  for (const impact of preview.ruleImpacts) {
    diagnostics.push(
      new vscode.Diagnostic(
        new vscode.Range(impact.line, 0, impact.line, 0),
        `Rule ${impact.rule} would change indentation by ${impact.delta} spaces.`,
        vscode.DiagnosticSeverity.Information
      )
    );
  }
}

  // if (state === "builtin") {
  //   const diag = new vscode.Diagnostic(
  //     new vscode.Range(0, 0, 0, 0),
  //     "Using VS Code's built-in HTML formatter.",
  //     vscode.DiagnosticSeverity.Information
  //   );
  //   diag.code = DiagnosticCodes.BuiltIn;
  //   diag.source = "Custom HTML Formatter";
  //   diagnostics.push(diag);
  // }
  
          // case DiagnosticCodes.BuiltIn:
        //   // switch to Custom HTML Formatter
        //   actions.push(this.enableDefaultsAction());
        //   // choose a formatter from vscode pop-up
        //   // actions.push(this.chooseFormatterAction());
        //   break;

old scripts:
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./ && node scripts/copy-fixtures.js",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint && npm run copy:fixtures",
    "lint": "eslint src",
    "test": "vscode-test",
    "copy:fixtures": "node scripts/copy-fixtures.js"
  },

